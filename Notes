_ couper les espaces entre quote/dquote
_ [new]copier les redir/heredoc, et paste les quotes/dquotes qui vont avec
_ [new]faire les modifs comme on l'entend sur les redir/heredoc(var env, quote/dquote)
_ remplacer les appel var env dans la string
_ remplacer les appel var env dans les espaces quote/dquote coupees
_ split pipeline
_ remplir struct cmd avec chaque pipeline
  _ (a modifier)open des files de redirection (avec les nouvelles donnes dans parsing)
  _ remplir struct exec
    _ split des arguments de la pipeline
    _ coller les espaces entre quote/dquote
